{
  "name": "cordova-plugin-fbsdk-repo",
  "private": true,
  "description": "Repository tooling only. The published package is in plugin/.",
  "scripts": {
    "format": "prettier --write .",
    "format:check": "prettier --check ."
  },
  "devDependencies": {
    "prettier": "3.6.2"
  }
}
